const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..','..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');const assert=(v,m)=>{if(!v){console.error('FAIL:',m);process.exit(1)}};const ui=read('components/public-chat.js');const api=read('public-chat.php');const router=read('router.php');const host=read('l8-html.php');assert(ui.includes('M 4 6 L 4 20'),'requested SVG button path missing');assert(ui.includes("document.querySelector('.top-bar-right')"),'chat button must mount in top bar');assert(ui.includes("bubble.textContent=msg.text"),'messages must render as textContent');assert(ui.includes('Cargar mensajes anteriores'),'history pagination missing');assert(api.includes("HASHCOD_PUBLIC_CHAT_TABLE = 'l8_durable_object_events'"),'append-only DB target missing');assert(api.includes('supabaseDbUpsert'),'server-side persistence missing');assert(api.includes('supabaseStorageUploadJson'),'storage mirror missing');assert(api.includes("securityRateAllowSliding('public_chat_write'"),'write rate limiting missing');assert(!api.includes("method === 'DELETE'"),'public chat must not expose delete');assert(router.includes("$uri === '/api/public-chat'"),'public chat route missing');assert(host.includes('hashcod-public-chat-inline'),'public chat must be injected');console.log('Public chat contract passed.');
(function () {
    'use strict';

    // RETIRED 2026-09-14
    // Signature controls belonged exclusively to the removed Toolbox circle-link
    // feature. Keep the compatibility filename inert for cached/older payloads.
    window.__hashcodToolboxCircleLinksRetired = true;
    window.__hashcodToolboxSignatureCopyLoaded = true;
})();

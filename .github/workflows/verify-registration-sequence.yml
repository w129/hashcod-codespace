name: Verify registration sequence

on:
  push:
    branches:
      - main
    paths:
      - components/platform-registration-form.js
      - components/platform-registration-form.css
      - registration-flip-build/**
      - Dockerfile
      - supabase.php
      - security.php
      - cloudflare-turnstile.php
      - platform-registration-contract.php
      - privacy.php
      - supabase/schema.sql
      - legal-evidence/registration-evidence-manifest.json
      - components/platform-entry-hold.js
      - components/platform-entry-hold.css
      - components/platform-entry-motion.js
      - components/admin-hello-button.js
      - l8-html.php
      - laragon-local-entry.php
      - tests/e2e/test_platform_registration_form.js
      - tests/e2e/test_platform_registration_sequence_playwright.js
      - tests/e2e/test_temporary_access_playwright.js
      - .github/workflows/verify-registration-sequence.yml
  pull_request:
    branches:
      - main
    paths:
      - components/platform-registration-form.js
      - components/platform-registration-form.css
      - registration-flip-build/**
      - Dockerfile
      - platform-registration-contract.php
      - privacy.php
      - supabase/schema.sql
      - legal-evidence/registration-evidence-manifest.json
      - components/platform-entry-hold.js
      - components/platform-entry-hold.css
      - components/admin-hello-button.js
      - l8-html.php
      - laragon-local-entry.php
      - tests/e2e/test_platform_registration_form.js
      - tests/e2e/test_platform_registration_sequence_playwright.js
      - tests/e2e/test_temporary_access_playwright.js
      - .github/workflows/verify-registration-sequence.yml
  workflow_dispatch:

jobs:
  real-registration-sequence:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build official Animate UI FlipButton
        working-directory: registration-flip-build
        run: |
          npm install --no-fund --no-audit
          npm run build
          test -s ../components/registration-flip.bundle.js

      - name: Syntax check registration and entry scripts
        run: |
          node --check components/platform-registration-form.js
          node --check components/platform-entry-hold.js
          php -l l8-html.php
          php -l laragon-local-entry.php
          php -l privacy.php
          php -l platform-registration-contract.php
          php -l cleanup-platform-registration.php
          node tests/e2e/test_platform_entry_hold.js
          node tests/e2e/test_platform_registration_form.js
          node --check tests/e2e/test_temporary_access_playwright.js

      - name: Install Chromium
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Verify real three-screen flow
        run: |
          php -S 127.0.0.1:8099 laragon-router.php >/tmp/hashcod-registration-sequence.log 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" >/dev/null 2>&1 || true' EXIT

          ready=0
          for attempt in $(seq 1 45); do
            if curl --fail --silent --show-error http://127.0.0.1:8099/ >/dev/null; then
              ready=1
              break
            fi
            sleep 1
          done

          if [ "$ready" -ne 1 ]; then
            echo "Hashcod test server did not become ready"
            cat /tmp/hashcod-registration-sequence.log || true
            exit 1
          fi

          REGISTRATION_SEQUENCE_URL=http://127.0.0.1:8099/ node tests/e2e/test_platform_registration_sequence_playwright.js
          TEMPORARY_ACCESS_URL=http://127.0.0.1:8099/ node tests/e2e/test_temporary_access_playwright.js


  live-registration-sequence:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Wait for Render to deploy this entry wiring
        env:
          LIVE_URL: https://hashcod-codespace-1.onrender.com/
        run: |
          deployed=0
          for attempt in $(seq 1 100); do
            html="$(curl --compressed --fail --silent --show-error --max-time 20 "$LIVE_URL" || true)"
            if printf '%s' "$html" | grep -q 'platform-entry-hold.js?v=20260918-37' \
              && printf '%s' "$html" | grep -q 'hashcodTemporaryAccessButton' \
              && printf '%s' "$html" | grep -q 'hashcodTemporaryAccessDialog' \
              && printf '%s' "$html" | grep -q 'data-hashcod-registration-flip="official"'; then
              echo "Render is serving the merged registration sequence."
              deployed=1
              break
            fi
            echo "Waiting for Render deployment ($attempt/100)…"
            sleep 6
          done

          if [ "$deployed" -ne 1 ]; then
            echo "Render did not expose the expected registration assets in time."
            exit 1
          fi

      - name: Install Chromium
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Verify registration on the public Render platform
        env:
          REGISTRATION_SEQUENCE_URL: https://hashcod-codespace-1.onrender.com/
          TEMPORARY_ACCESS_URL: https://hashcod-codespace-1.onrender.com/
        run: |
          node tests/e2e/test_platform_registration_sequence_playwright.js
          node tests/e2e/test_temporary_access_playwright.js

      - name: Verify retired registration persistence endpoint
        run: |
          node - <<'NODE'
          const response = await fetch('https://hashcod-codespace-1.onrender.com/api/platform-registration', {
            headers: { 'Accept': 'application/json' },
            redirect: 'manual'
          });
          console.log(JSON.stringify({ http_status: response.status }));
          if (response.status >= 200 && response.status < 300) {
            throw new Error('Retired registration persistence endpoint is still active.');
          }
          NODE

'use strict';

// Hashcod Desktop now starts only the bundled Hashcod Codespace runtime.
// God's Eye View executes inside the renderer and does not require a sidecar
// Node process, localhost port, or external browser handoff.
require('./main.js');
